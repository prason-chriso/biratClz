<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.prason.biratclz">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainPageRegulator"></activity>
        <activity android:name=".SignupPage"></activity>
        <activity android:name=".PanelFragmentRegulator"></activity>
        <activity android:name=".OptionDownloadUploadManaging"></activity>
        <activity android:name=".OptionEnquiryReviewingFragUPdated"></activity>

        <service
            android:name=".MyServiceProvider"
            android:enabled="true"
            android:exported="false"></service>

        <service
            android:name=".MyServiceClassControllingLayoutUpdate"
            android:enabled="true"
            android:exported="false"></service>
    </application>

</manifest>